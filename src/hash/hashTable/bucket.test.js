const Bucket = require('./bucket');

describe('Bucket Tests (Simple Linked List)', () => {
    
  describe('Should be able to instantiate a new bucket with the new keyword.', () => {
    
    test('Should be able to create a new bucket.', () => {
      
    })

  })

})